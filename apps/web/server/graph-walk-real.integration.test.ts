import { describe, it } from 'vitest';

describe.skip('real ingestion graph walking', () => {
  it('is skipped because legacy graph-walk eval retired after LGS cutover', () => {
    // Skipped
  });
});
