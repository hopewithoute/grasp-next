import { describe, it } from 'vitest';

describe.skip('Refinement Agent - Real Provider (Graph Proposals)', () => {
  it('is skipped because legacy refinement eval retired after LGS cutover', () => {
    // Skipped
  });
});
