// Parallel-route default for the projectChrome slot.
// Renders nothing on non-project dashboard URLs (e.g. /dashboard/projects list).
export default function ProjectChromeDefault() {
  return null;
}
