[project]
name = "grasp-embedding-sidecar"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "llama-cpp-python[server]",
  "pydantic",
  "tiktoken>=0.13.0",
  "uvicorn",
]

[tool.uv]
package = false
