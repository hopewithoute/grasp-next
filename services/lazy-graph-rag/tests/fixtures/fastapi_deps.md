# FastAPI Dependency Injection

FastAPI handles dependency injection using the Depends function. You declare the dependency in the route signature, and FastAPI automatically resolves it before executing the route logic.