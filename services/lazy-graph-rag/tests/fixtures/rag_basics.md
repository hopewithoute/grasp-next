# Retrieval-Augmented Generation

RAG consists of two main components: retrieval and generation. The retrieval component searches a database for relevant information, and the generation component uses that information to construct a final answer.